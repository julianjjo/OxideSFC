//! Renderer tests, grouped to mirror the production modules. Most drive a
//! full `render_frame` and assert on output pixels, with shared fixtures in
//! `common`.

mod background;
mod color;
mod common;
mod frame;
mod mode7;
mod sprites;
mod windows;
