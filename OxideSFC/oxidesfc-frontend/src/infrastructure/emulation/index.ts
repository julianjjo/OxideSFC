/**
 * Emulation Infrastructure Module
 *
 * The Tauri-command-backed emulation core/service implementations that used
 * to live here (TauriEmulationCore, EmulationService) were dead code with no
 * callers — the app talks to the backend directly via
 * `src/stores/emulationStore.ts`. They were removed; this module is
 * currently intentionally empty.
 */
export {};
